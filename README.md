# Inicijalizovati MySQl i izvrsiti sql skriptu.
# U application.properties podesiti jdbc.username i jdbc.password
# Aplikaciju pokrenuti komandom mvn jetty:run
# U browseru ici na http://localhost:8080
